/**
 * Board
 * - Get boards
 * - Add board
 * - Remove board
 * - Update board
 *
 */

const getBoards = () => {};

const addBoard = (board) => {};

const removeBoard = (boardTitle) => {};

const updateBoard = (board) => {};
