/**
 * Task
 * - Add task to board
 * - Remove task from board
 * - update task from board
 *
 */

const addTaskToBoard = (task, boardName) => {};

const removeTaskFromBoard = (task, boardName) => {};

const updateTaskFromBoard = (task, boardName) => {};
